<section class="contacto" id="contacto">
  <div class="contacto-header">
    <h2>Contacto</h2>
    <p class="section-intro">
      Para solicitar cotizaciones, información técnica o visita a obra, 
      contacte directamente por WhatsApp o correo electrónico.
    </p>
  </div>

  <div class="contacto-grid">
    <div class="contacto-card">
      <div class="contacto-icon">
        <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
        </svg>
      </div>
      <h3>WhatsApp</h3>
      <p class="contacto-value">+52 81 8690 8682</p>
      <a class="btn" href="https://wa.me/528186908682?text=Hola,%20me%20gustaría%20información%20sobre%20un%20servicio">
        Iniciar conversación
      </a>
    </div>

    <div class="contacto-card">
      <div class="contacto-icon">
        <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
          <polyline points="22,6 12,13 2,6"/>
        </svg>
      </div>
      <h3>Correo Electrónico</h3>
      <p class="contacto-value">serviciosindustriales.garcia@outlook.com</p>
      <a class="btn btn-secondary" href="mailto:serviciosindustriales.garcia@outlook.com">
        Enviar correo
      </a>
    </div>

    <div class="contacto-card">
      <div class="contacto-icon">
        <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/>
          <circle cx="12" cy="10" r="3"/>
        </svg>
      </div>
      <h3>Ubicación</h3>
      <p class="contacto-value">
        Monterrey, Nuevo León<br/>
        <span class="contacto-subtext">Área metropolitana y municipios aledaños</span>
      </p>
    </div>
  </div>

  <div class="contacto-footer">
    <div class="horario-info">
      <h3>Horario de Atención</h3>
      <div class="horario-grid">
        <div class="horario-item">
          <span class="horario-dia">Lunes a Viernes:</span>
          <span class="horario-hora">8:00 AM - 6:00 PM</span>
        </div>
        <div class="horario-item">
          <span class="horario-dia">Sábados:</span>
          <span class="horario-hora">9:00 AM - 2:00 PM</span>
        </div>
      </div>
      <p class="horario-note">
        Tiempo de respuesta: cotizaciones en 24-48 horas hábiles. 
        Emergencias industriales atendidas según disponibilidad.
      </p>
    </div>
  </div>
</section>

<style>
.contacto {
  background: var(--dark);
}

.contacto-header {
  margin-bottom: 4rem;
}

.section-intro {
  font-size: 1.25rem;
  line-height: 1.6;
  color: var(--text-secondary);
  max-width: 800px;
  margin-top: 1.5rem;
}

.contacto-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 2.5rem;
  margin-bottom: 4rem;
}

.contacto-card {
  background: var(--gray-medium);
  padding: 2.5rem 2rem;
  border: 1px solid var(--border);
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  transition: border-color 0.2s ease;
}

.contacto-card:hover {
  border-color: var(--orange);
}

.contacto-icon {
  width: 64px;
  height: 64px;
  background: rgba(196, 90, 26, 0.1);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 1.5rem;
}

.contacto-icon svg {
  color: var(--orange);
}

.contacto-card h3 {
  font-size: 1.125rem;
  color: var(--text-primary);
  margin-bottom: 1rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.contacto-value {
  font-size: 1.125rem;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 1.5rem;
  line-height: 1.5;
}

.contacto-subtext {
  font-size: 0.9375rem;
  font-weight: 400;
  color: var(--text-muted);
  display: block;
  margin-top: 0.5rem;
}

.contacto-footer {
  background: var(--gray-light);
  padding: 2.5rem;
  border-top: 3px solid var(--orange);
}

.horario-info h3 {
  font-size: 1.25rem;
  color: var(--text-primary);
  margin-bottom: 1.5rem;
  text-align: center;
}

.horario-grid {
  display: flex;
  justify-content: center;
  gap: 3rem;
  margin-bottom: 1.5rem;
  flex-wrap: wrap;
}

.horario-item {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  text-align: center;
}

.horario-dia {
  font-size: 0.9375rem;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.horario-hora {
  font-size: 1.125rem;
  font-weight: 600;
  color: var(--text-primary);
}

.horario-note {
  text-align: center;
  font-size: 0.9375rem;
  color: var(--text-muted);
  max-width: 600px;
  margin: 0 auto;
  line-height: 1.6;
}

@media (max-width: 768px) {
  .contacto-grid {
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  .contacto-card {
    padding: 2rem 1.5rem;
  }

  .contacto-footer {
    padding: 2rem 1.5rem;
  }

  .horario-grid {
    flex-direction: column;
    gap: 1.5rem;
  }
}
@media (max-width: 1024px) {
  .contacto-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
  }

  .contacto-card:last-child {
    grid-column: 1 / -1;
  }
}

@media (max-width: 768px) {
  .contacto-header {
    margin-bottom: 3rem;
  }

  .section-intro {
    font-size: 1.125rem;
  }

  .contacto-grid {
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  .contacto-card {
    padding: 2rem 1.5rem;
  }

  .contacto-card:last-child {
    grid-column: 1;
  }

  .contacto-icon {
    width: 56px;
    height: 56px;
  }

  .contacto-icon svg {
    width: 28px;
    height: 28px;
  }

  .contacto-card h3 {
    font-size: 1rem;
  }

  .contacto-value {
    font-size: 1.0625rem;
  }

  .contacto-footer {
    padding: 2rem 1.5rem;
  }

  .horario-grid {
    flex-direction: column;
    gap: 1.5rem;
  }

  .horario-note {
    font-size: 0.875rem;
  }
}

@media (max-width: 480px) {
  .contacto-grid {
    gap: 1.5rem;
  }

  .contacto-card {
    padding: 1.75rem 1.25rem;
  }

  .contacto-footer {
    padding: 1.75rem 1.25rem;
  }

  .horario-info h3 {
    font-size: 1.125rem;
  }
}
</style>